<view class="segment-picker">
  <view
    wx:for="{{options}}"
    wx:key="value"
    class="segment-picker__item {{value === item.value ? 'segment-picker__item--active' : ''}}"
    data-value="{{item.value}}"
    bindtap="handleSelect"
  >
    <text>{{item.label}}</text>
  </view>
</view>
